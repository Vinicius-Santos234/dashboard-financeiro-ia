# Spec 001 — Dashboard Financeiro v1

**Status:** aprovada, não iniciada
**Data:** 2026-09-05
**Repositório:** `dashboard-financeiro` (provisório)
**Documento de origem:** `Desktop\Automações\projetos\dashboard-financeiro.md`

---

## 1. Objetivo

Aplicação web onde o usuário faz login, envia o extrato exportado do próprio banco
(OFX ou CSV), e vê os gastos categorizados por IA em gráficos interativos, com insights
em texto sobre o mês.

**Uma frase de sucesso:** um estranho abre o link, entra na conta demo, e em menos de
30 segundos entende no que aquele dinheiro foi gasto — sem eu explicar nada.

---

## 2. Fora de escopo da v1

Escrito antes do resto porque cada item é uma porta que, aberta, adia o primeiro gráfico
em semanas.

| Fora | Por quê |
|---|---|
| Open Finance (Pluggy/Belvo) | Exige aprovação de terceiro. Entra na v2 como segunda `SourceAdapter` |
| Bot de WhatsApp | O `source` já existe no schema; o bot é outro projeto |
| Investimentos | Outro domínio inteiro (posição, rentabilidade, cotação) |
| App mobile | O dashboard é responsivo; app nativo é outro ciclo |
| Orçamentos e metas | Precisa de um modelo de planejamento que a v1 não tem |
| Compartilhamento entre pessoas | Cada usuário vê só o seu. Família/casal é v3+ |
| Multi-moeda | Tudo em BRL, em centavos |
| Exportação de relatório (PDF/Excel) | A tela é o produto na v1 |
| Conciliação bancária | Não é o problema que este app resolve |

> Se algo desta lista aparecer durante o desenvolvimento, a resposta é "v2" — não
> "rapidinho".

---

## 3. Decisões travadas

Nenhuma destas volta a ser discutida sem um motivo novo e escrito.

| # | Decisão | Escolha | Motivo |
|---|---|---|---|
| D1 | Stack | Next.js (App Router), TypeScript strict | Um repositório; Route Handler é o servidor onde a chave da IA fica |
| D2 | Dados + auth | Supabase — Postgres, Auth, RLS | RLS é o mecanismo demonstrável de isolamento; cabe no README como SQL |
| D3 | Onde a IA roda | Servidor, sempre | Chave no cliente vaza. Nenhuma chamada a LLM sai do browser |
| D4 | LLM | Gemini 2.x Flash com `responseSchema` | Free tier real para iterar o prompt; JSON garantido por schema |
| D5 | Fonte v1 | OFX + CSV atrás de `SourceAdapter` | Não depende de aprovação de ninguém; mesmo encaixe do Open Finance |
| D6 | Categorias | Enum fechado de 10 + correção manual → regra | Categoria livre destrói a comparação mês a mês |
| D7 | Privacidade | Anonimizar antes da chamada; IA recebe id opaco | Categorizar precisa de descrição e valor, não de identidade |
| D8 | Entrega | Deploy Vercel + conta demo + README dos mecanismos | Sem link aberto, o portfólio não é verificável |
| D9 | Dinheiro | `bigint` em centavos, negativo = saída | `float` erra centavo e só aparece quando o total não bate |
| D10 | Gráficos | Recharts | Bom o bastante, React-nativo, pizza clicável sem ginástica |
| D11 | UI | Tailwind + shadcn/ui | Componentes acessíveis sem virar projeto de design system |
| D12 | Testes | Vitest | Parser e anonimizador são os dois que quebram em silêncio |

---

## 4. Modelo de dados

### 4.1 Enum de categorias

```sql
create type categoria as enum (
  'alimentacao', 'transporte', 'moradia', 'saude', 'lazer',
  'educacao', 'compras', 'contas_fixas', 'receita', 'outros'
);
```

Dez, fechadas. `receita` cobre entrada de dinheiro; `outros` é o escape — e a taxa de
`outros` é uma métrica de qualidade, não um detalhe (ver §9).

### 4.2 Tabelas

```sql
create table accounts (
  id           uuid primary key default gen_random_uuid(),
  user_id      uuid not null references auth.users(id) on delete cascade,
  name         text not null,
  institution  text,
  kind         text not null check (kind in ('checking','savings','credit_card')),
  created_at   timestamptz not null default now()
);

create table imports (
  id              uuid primary key default gen_random_uuid(),
  user_id         uuid not null references auth.users(id) on delete cascade,
  account_id      uuid references accounts(id) on delete set null,
  source          text not null check (source in ('ofx','csv')),
  filename        text not null,
  file_hash       text not null,          -- sha256 do arquivo, só para avisar reimport
  period_start    date,
  period_end      date,
  rows_total      integer not null default 0,
  rows_imported   integer not null default 0,
  rows_duplicated integer not null default 0,
  status          text not null check (status in ('parsed','categorized','failed')),
  error           text,
  created_at      timestamptz not null default now()
);

create table transactions (
  id                uuid primary key default gen_random_uuid(),
  user_id           uuid not null references auth.users(id) on delete cascade,
  account_id        uuid not null references accounts(id) on delete cascade,
  import_id         uuid references imports(id) on delete set null,
  occurred_on       date not null,
  amount_cents      bigint not null,      -- negativo = saída, positivo = entrada
  description_raw   text not null,        -- como veio do banco, nunca sai do servidor
  description_clean text not null,        -- anonimizada; é esta que vai para a LLM
  fitid             text,                 -- id do banco, quando o OFX traz
  fingerprint       text not null,
  category          categoria,
  category_source   text check (category_source in ('ai','rule','user')),
  confidence        numeric(3,2),
  source            text not null default 'ofx'
                    check (source in ('ofx','csv','bot','openfinance')),
  ai_opt_out        boolean not null default false,  -- nunca enviar à LLM (§7.2)
  created_at        timestamptz not null default now(),
  unique (user_id, fingerprint)
);

create table rules (
  id         uuid primary key default gen_random_uuid(),
  user_id    uuid not null references auth.users(id) on delete cascade,
  pattern    text not null,               -- substring normalizada, ex: 'IFOOD'
  category   categoria not null,
  hits       integer not null default 0,
  created_at timestamptz not null default now(),
  unique (user_id, pattern)
);

create table insights (
  id           uuid primary key default gen_random_uuid(),
  user_id      uuid not null references auth.users(id) on delete cascade,
  period       date not null,             -- sempre dia 1 do mês
  body         jsonb not null,            -- { headline, items: [...] }
  model        text not null,
  generated_at timestamptz not null default now(),
  unique (user_id, period)
);
```

### 4.3 A deduplicação — o detalhe que morde na segunda importação

Reimportar o mesmo extrato **dobra o gráfico** se não houver `fingerprint`. Duas fontes,
dois tratamentos:

```
OFX com FITID:  fingerprint = 'ofx:' || account_id || ':' || fitid
qualquer outro: fingerprint = 'h:' || sha256(account_id|occurred_on|amount_cents|desc_norm|seq)
```

`seq` existe por um motivo específico: **dois cafés de R$ 8,00 no mesmo lugar no mesmo dia
são duas transações legítimas e idênticas.** Sem `seq`, a segunda seria descartada como
duplicata. `seq` é o índice da ocorrência daquele tuplo idêntico *dentro do arquivo*
(0, 1, 2…), calculado na ordem em que aparecem.

`desc_norm` = maiúsculas, sem acento, espaços colapsados. Só para o hash — não substitui
a descrição guardada.

### 4.4 RLS — o mecanismo, não a promessa

Para **todas** as cinco tabelas:

```sql
alter table transactions enable row level security;

create policy "own rows only" on transactions
  for all
  using      (auth.uid() = user_id)
  with check (auth.uid() = user_id);
```

O `with check` não é enfeite: sem ele um usuário autenticado poderia **inserir** linhas
com o `user_id` de outro. `using` protege a leitura, `with check` protege a escrita.

A `service_role` key nunca é usada em código de request — só em scripts de seed. Se um
Route Handler precisar dela, o desenho está errado.

---

## 5. Arquitetura

```
app/
  (auth)/login/page.tsx
  (app)/dashboard/page.tsx          gráficos + insights do mês
  (app)/transacoes/page.tsx         lista, filtro, correção de categoria
  (app)/importar/page.tsx           upload e resultado do import
  (app)/conta/page.tsx              dados da conta e exclusão (LGPD)
  api/imports/route.ts              recebe arquivo, parseia, dedupe, persiste
  api/categorize/route.ts           lote → Gemini → grava categoria
  api/insights/route.ts             gera/lê o insight do mês
lib/
  sources/    types.ts  ofx.ts  csv.ts        SourceAdapter e implementações
  privacy/    anonymize.ts                    o que nunca sai da máquina
  llm/        provider.ts  gemini.ts  schema.ts
  domain/     money.ts  fingerprint.ts  categories.ts  rules.ts
  supabase/   server.ts  client.ts
supabase/migrations/
tests/        fixtures/  *.test.ts
```

### 5.1 O contrato que o Open Finance vai reusar

```ts
// lib/sources/types.ts
export interface RawTransaction {
  occurredOn: string        // ISO 'YYYY-MM-DD'
  amountCents: number       // negativo = saída
  description: string       // como veio, sem tratamento
  fitid?: string
}

export interface ParseResult {
  transactions: RawTransaction[]
  account?: ContaDetectada          // ACCTID, instituição, tipo, moeda
  periodStart?: string
  periodEnd?: string
  descartadas: LinhaDescartada[]    // linha, conteúdo, motivo
}

export interface SourceAdapter<Opcoes = void> {
  readonly id: 'ofx' | 'csv' | 'bot' | 'openfinance'
  parse(input: ArrayBuffer | string, opcoes: Opcoes): Promise<ParseResult>
}
```

Este é o ponto inteiro da inversão de ordem: quando o Open Finance entrar, ele implementa
`SourceAdapter` e **nada mais no app muda**.

> **Revisão em 05/09, ao implementar (E2):** o contrato original devolvia
> `RawTransaction[]` puro. Duas coisas obrigaram a mudar, e as duas apareceram só ao
> escrever o parser:
>
> - **`periodStart`/`periodEnd`/`account`** — a tabela `imports` tem essas colunas e o
>   dado só existe dentro do arquivo. Sem devolvê-los, o Route Handler teria que reparsear.
> - **`descartadas`** — a linha que o parser não consegue ler não pode sumir calada. É a
>   mesma doença do `parseAmountToCents` chutando um valor: o total do dashboard diverge
>   do extrato e não há pista de onde. Cada descarte carrega número da linha e motivo.
>
> O parâmetro `Opcoes` existe porque o CSV precisa do mapeamento de colunas e o OFX não
> precisa de nada.

### 5.2 O pipeline, na ordem em que o dado atravessa

```
arquivo → parse → normalizar → fingerprint → dedupe → persistir (sem categoria)
                                                            ↓
                              regras do usuário casam? ──sim──→ grava categoria (0 tokens)
                                                            ↓ não
                                          anonimizar → Gemini (lote 50) → validar → gravar
```

O arquivo **nunca é gravado em disco nem em bucket**. É parseado em memória e descartado
ao fim do request. Só as linhas persistem.

---

## 6. A camada de IA

### 6.1 O que sai da máquina

Só isto, e nada mais:

```json
[
  { "id": "t_01", "desc": "IFD*IFOOD SAO PAULO", "v": -4790, "d": "2026-08-14" },
  { "id": "t_02", "desc": "PIX ENVIADO",         "v": -5000, "d": "2026-08-14" }
]
```

`id` é opaco e vive só durante a chamada — não é o uuid da transação. O backend guarda o
mapa `id → uuid` em memória e recasa a resposta.

### 6.2 O schema da resposta

```ts
{
  responseMimeType: 'application/json',
  responseSchema: {
    type: 'array',
    items: {
      type: 'object',
      properties: {
        id:         { type: 'string' },
        category:   { type: 'string', enum: [/* as 10 */] },
        confidence: { type: 'number' }
      },
      required: ['id', 'category', 'confidence']
    }
  }
}
```

`responseSchema` reduz o risco de JSON quebrado — não elimina. A resposta passa por Zod
no servidor. Item com `id` desconhecido é descartado; transação sem resposta vira
`outros` com `confidence: 0` e `category_source: 'ai'`.

### 6.3 Custo e limites

- Lotes de **50 transações** por chamada.
- Teto de **20 lotes por import** (1.000 transações). Acima disso, o import é recusado
  com mensagem clara em vez de queimar cota em silêncio.
- Transação que casa com `rule` **não entra no lote**. Um mês já corrigido custa zero.

### 6.4 Insights

Gerados sob demanda por mês, uma chamada, gravados em `insights` e reexibidos do banco.
Botão "regerar" explícito. A entrada é o **agregado** (total por categoria no mês e no
mês anterior), nunca a lista de transações — insight não precisa ver linha a linha.

```ts
type InsightBody = {
  headline: string
  items: { text: string; severity: 'info' | 'atencao'; category: Categoria | null }[]
}
```

---

## 7. Privacidade — o que nunca sai da máquina

### 7.1 Regras de anonimização

Aplicadas em `description_clean` no servidor, antes de qualquer chamada:

| Alvo | Tratamento |
|---|---|
| CPF `\d{3}\.?\d{3}\.?\d{3}-?\d{2}` | removido |
| CNPJ `\d{2}\.?\d{3}\.?\d{3}/?\d{4}-?\d{2}` | removido |
| Agência/conta `AG\s*\d{4}`, `C/?C\s*[\d-]+` | removido |
| Sequência de 6+ dígitos | removida |
| E-mail, telefone, chave PIX aleatória (uuid) | removidos |
| Contraparte após `PIX ENVIADO`/`PIX RECEBIDO`/`TED`/`DOC`/`TRANSFERENCIA` | truncado — fica só a operação |

Nome de estabelecimento **permanece**, porque é exatamente o que permite categorizar.

### 7.2 O risco residual, dito em voz alta

Nome de estabelecimento pode revelar coisa sensível — uma clínica, uma farmácia, um
advogado. A v1 não resolve isso e **não vai fingir que resolve**. Fica registrado no
README como limitação conhecida, com a saída disponível ao usuário: marcar uma transação
como "não enviar à IA", que a deixa em `outros` sem nunca sair do servidor.

### 7.3 O teste que transforma promessa em asserção

```ts
// tests/anonymize.test.ts
it('nenhum payload de saída contém identificador', () => {
  for (const linha of fixtures) {
    const out = anonymize(linha)
    expect(out).not.toMatch(/\d{3}\.?\d{3}\.?\d{3}-?\d{2}/)          // CPF
    expect(out).not.toMatch(/\d{2}\.?\d{3}\.?\d{3}\/?\d{4}-?\d{2}/)  // CNPJ
    expect(out).not.toMatch(/\d{6,}/)
    for (const t of IDENTIDADE_DO_DONO) expect(out).not.toContain(t)
  }
})
```

O teste roda sobre fixtures derivadas de extrato real (§10). Sem ele, "o app anonimiza"
é uma frase no README.

### 7.4 LGPD

- **Exclusão de conta** em `/conta`: confirmação por digitação, `delete` do usuário no
  Supabase Auth, cascade real apagando as cinco tabelas. Zero linha órfã.
- **Retenção de arquivo:** nenhuma. O arquivo enviado não é persistido.
- **Base legal e transferência internacional:** a descrição anonimizada vai para o Google.
  Dito no README e na tela de import — não escondido.

---

## 8. Etapas e critério de aceite

Cada etapa é demonstrável sozinha. Nenhuma começa antes da anterior passar.

### E1 — Scaffold com RLS ligada no primeiro commit
- [ ] Next.js + TS strict + Tailwind + shadcn/ui rodando
- [ ] Projeto Supabase criado, migration `0001` com as 5 tabelas, enum e **policies**
- [ ] Login e logout funcionando
- **Aceite:** com dois usuários criados, um `select * from transactions` autenticado como
  A **não retorna nenhuma linha** de B. Provado por teste de integração, não por inspeção.

### E2 — Import e lista crua (sem IA, sem gráfico)
- [ ] `OfxAdapter` e `CsvAdapter` implementando `SourceAdapter`
- [ ] CSV com tela de mapeamento de colunas (data / valor / descrição)
- [ ] Fingerprint, dedupe e contagem em `imports`
- [ ] `/transacoes` listando data, descrição e valor formatado
- **Aceite:** importar o mesmo OFX real **duas vezes** resulta em
  `rows_imported > 0, rows_duplicated = 0` na primeira e
  `rows_imported = 0, rows_duplicated = N` na segunda, com a lista **inalterada**.
- **Aceite:** um extrato com dois lançamentos idênticos no mesmo dia importa **os dois**.
- **Aceite:** nenhuma linha do arquivo desaparece sem registro. O que o parser não
  conseguiu ler aparece em `descartadas` com o número da linha e o motivo, e a tela de
  import mostra a contagem. Linha sumindo calada faz o total divergir do extrato sem
  pista de onde.
- **Aceite:** um arquivo em windows-1252 (que é como boa parte dos bancos brasileiros
  exporta) devolve `PADARIA SÃO JOÃO`, não `PADARIA S<?>O JO<?>O`. O que a LLM recebe
  para categorizar depende disto.

### E3 — Anonimizador, antes da primeira chamada de LLM
- [ ] `lib/privacy/anonymize.ts` com as regras de §7.1
- [ ] Suíte de §7.3 passando sobre fixtures de extrato real
- **Aceite:** a suíte é verde e falha se qualquer regra for removida. Esta etapa é
  **pré-requisito bloqueante** de E4 — nenhuma chamada ao Gemini antes dela passar.

### E4 — Categorização
- [ ] `GeminiProvider` implementando `LLMProvider`, com `responseSchema`
- [ ] Lotes de 50, teto de 20 lotes, validação Zod da resposta
- [ ] Consulta a `rules` antes do lote
- [ ] Correção de categoria em `/transacoes`, criando `rule` com o padrão sugerido e editável
- **Aceite:** num extrato real de 1 mês, **≥ 80% das transações** saem com categoria
  diferente de `outros`. Abaixo disso o prompt é retrabalhado antes de seguir.
- **Aceite:** corrigir uma transação e reimportar o mês seguinte categoriza a ocorrência
  equivalente **pela regra**, com `category_source = 'rule'` e zero chamada ao Gemini.

### E5 — Dashboard
- [ ] Pizza por categoria no mês, clique abrindo a lista filtrada daquela fatia
- [ ] Cartões: total gasto, total recebido, saldo, maior categoria
- [ ] Comparação com o mês anterior por categoria
- [ ] Seletor de mês
- **Aceite:** a soma das fatias bate **exatamente** com o total de saídas do mês, ao
  centavo. Divergência aqui é bug de arredondamento e reprova a etapa.

### E6 — Insights
- [ ] `/api/insights` gerando por mês a partir do agregado, cache em `insights`, botão regerar
- [ ] Exibição no dashboard com marcação de severidade
- **Aceite:** abrir o dashboard duas vezes no mesmo mês faz **uma** chamada ao Gemini.

### E7 — Deploy e README
- [ ] Deploy na Vercel, Supabase de produção, env vars separadas de dev
- [ ] Conta demo com seed (§10)
- [ ] README com: a policy de RLS em SQL, o pipeline de anonimização, o que acontece na
      exclusão de conta, e a limitação residual de §7.2
- **Aceite:** um estranho abre o link, entra no demo e vê o dashboard populado **sem
  configurar nada**.

---

## 9. Métricas de qualidade

Medidas a partir de E4, num extrato real de 1 mês:

| Métrica | Meta |
|---|---|
| Transações fora de `outros` | ≥ 80% |
| Categorias corrigidas manualmente | ≤ 15% |
| Chamadas ao Gemini no 2º mês (com regras já criadas) | ≤ 60% do 1º mês |
| Duplicatas em reimport | 0 |

A taxa de `outros` é o termômetro do prompt. Se subir, o problema é o prompt, não o banco.

---

## 10. Dados de desenvolvimento e conta demo

**São duas coisas diferentes e não se misturam.**

### Desenvolvimento — extrato real
Extratos reais do usuário, em `tests/fixtures/`, **fora do git** (`.gitignore`). São a
única forma de descobrir que cada banco escreve a mesma coisa de um jeito
(`Pagamento de fatura` × `PAGTO FATURA`). As fixtures versionadas são derivadas destas,
já anonimizadas.

### Conta demo — seed derivado, nunca o extrato cru
A conta demo fica atrás de um link público. O seed é **gerado a partir** do extrato real
por um script `scripts/seed-demo.ts`:

- descrições de estabelecimento mantidas (é o que dá realismo)
- toda linha passa pelo `anonymize` de §7.1 antes de entrar
- datas deslocadas por um offset fixo
- valores multiplicados por um fator (ex.: 0,83) e re-arredondados

Resultado: um extrato que **parece real porque é real na forma**, sem ser o do usuário.

> Isto é uma decisão de segurança, não de conveniência: o que entra num link público não
> volta atrás. Se a preferência mudar para usar o extrato cru no demo, é este parágrafo
> que muda — e a consequência precisa estar clara.

---

## 11. Riscos

| Risco | Mitigação |
|---|---|
| Banco exporta OFX fora do padrão | `OfxAdapter` tolerante + fallback para CSV com mapeamento |
| Gemini devolve JSON fora do schema | Zod no servidor; item inválido vira `outros`, nunca quebra o import |
| Free tier do Gemini acaba no meio do desenvolvimento | `LLMProvider` é interface; trocar provedor é uma classe nova |
| Prompt categoriza mal PIX e transferência | Previsto: são os campos mais pobres. As `rules` do usuário são a saída |
| Extrato grande estoura o timeout da Route Handler na Vercel | Teto de 1.000 transações por import; acima disso, recusa explícita |
| Reimport duplicando | `fingerprint` unique + teste de aceite em E2 |

---

## 12. Pendências

- [ ] Nome definitivo do repositório — segue `dashboard-financeiro` até decidir
- [x] Formato da conta demo — **seed derivado do extrato real** (§10)
- [ ] Confirmar o modelo exato do Gemini e o limite do free tier no dia de E4
- [ ] Decidir se `/conta` mostra histórico de imports (provável: sim, é barato)

---

## 13. Referências

- Documento de origem: `Desktop\Automações\projetos\dashboard-financeiro.md`
- Nota do projeto no vault: `Documents\Claude\Projetos\dashboard-financeiro.md`
- Método: `Documents\Claude\Pesquisa\spec-driven-development.md`
